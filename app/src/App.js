import styled from 'styled-components';

const Div = styled.div`
	text-align: center;
`;

export const App = () => {
	return (
		<Div className="App">
			<i className="fa fa-camera-retro"></i>
		</Div>
	);
};
